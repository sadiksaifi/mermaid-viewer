export const ZOOM_CONFIG = {
  initialScale: 1,
  minScale: 0.1,
  maxScale: 8,
  wheelStep: 0.2,
  wheelSmoothStep: 0.01,
  buttonStep: 0.5,
  animationTime: 200,
} as const;

export const EXPORT_CONFIG = {
  scale: 4,
} as const;
